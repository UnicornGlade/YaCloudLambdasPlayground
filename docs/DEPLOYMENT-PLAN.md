# План первого облачного деплоя

## Решения

- Один репозиторий, Nuxt + Nitro с пресетом `aws_lambda`.
- Один хост Managed PostgreSQL без реплик, 10 ГБ `network-ssd`. 22 сентября API не нашёл `c4a-c2-m4`: после согласования создан `s3-c2-m8`, сейчас STOPPED между занятиями; см. [DATABASE-PLAN.md](DATABASE-PLAN.md).
- Один бакет сайта, отдельный бакет больших файлов, отдельное приватное служебное хранилище state/артефактов.
- Веб-страница и `/api/*` через один API Gateway. Большие файлы — напрямую из Object Storage.
- Адрес **подтверждён**: `playground.unicornglade.tech`. Корневую A-запись не менять. Пошаговая инструкция: [DOMAIN-SETUP.md](DOMAIN-SETUP.md).
- Владелец подтвердил перенос DNS всего `unicornglade.tech` в Yandex Cloud. Зона и записи созданы Terraform, корневые A/TXT сохранены. Владелец сменил NS у регистратора; `ns1.yandexcloud.net`, `ns2.yandexcloud.net` и CNAME проверки уже видны через внешний DNS. Будущая почта Яндекс 360 добавляется записями в ту же зону.
- Никаких Kubernetes, постоянно работающего веб-сервера или CDN на старте.
- Инфраструктура описывается Terraform. Сборка и загрузка файлов — отдельные операции внутри одной команды деплоя, а не отдельный Terraform resource для каждого JS-файла.

Обновлённый ориентир только для БД `s3-c2-m8` + 10 ГБ SSD: 5 784,48 ₽ за 30 дней постоянной работы или 547,76–938,56 ₽ при 50–100 часах работы. Прежняя оценка ~4 400 ₽ использовала недоступный в нашем API класс. После решения о Cloud DNS дополнительно около 43 ₽/месяц за зону и 37,94 ₽/миллион авторитетных запросов. State оплачивается по тарифам/бесплатным лимитам S3 (сейчас килобайты). Это оценка, не лимит. Трафик архивов и превышение бесплатных квот оплачиваются отдельно.

## Этап 1 — приложение без облачных изменений

- [x] Nuxt-страница, API, миграция.
- [x] Проверка типов и unit-тесты.
- [x] Два артефакта: статический сайт и серверный ZIP.
- [x] Локальный вызов настоящей Lambda-сборки в изоляции от node_modules репозитория.
- [ ] Интеграционный тест с настоящим PostgreSQL: нужен локальный сервер БД.

## Этап 2 — доступ, выбор каталога и Terraform

- [x] CLI обновлён до 1.34.0, браузерная авторизация работает. Новые OAuth-токены Яндекс ID больше не использовать.
- [x] Проверены cloud и отдельный каталог `lambda-playground-folder` (`b1gh1fk7hbuj5hu9tujb`).
- [ ] Проверить платежный аккаунт и доступные квоты.
- [x] Подтверждено доменное имя `playground.unicornglade.tech`.
- [x] Подтверждён учебный режим с явной остановкой БД между занятиями.
- [x] Локально установлен Terraform 1.16.2 с проверкой SHA-256, провайдер 0.228.0 закреплён в `infra/certificate/.terraform.lock.hcl`.
- [x] Приватный S3 backend создан с versioning, запретом HTTP и доступом конкретному владельцу. State bootstrap и сертификата/DNS перенесён из local backend. Эфемерные ключи ограничены бакетом, native S3 locking проверен конкурентным тестом. Анонимный HEAD state возвращает 403. См. `TERRAFORM.md`.
- [ ] Разделить права управления инфраструктурой, публикации артефактов и чтения диагностики.
- [ ] Для секретов использовать Lockbox, учесть попадание некоторых значений в Terraform state даже при sensitive=true.
- [x] Перед созданием служебного бакета и DNS-зоны показаны планы и оценка стоимости. Для платной инфраструктуры приложения потребуется новый план.

## Этап 3 — минимальный облачный вызов и HTTPS

Облачный запуск без БД выполнен. Проверены статика, реальный вызов функции через шлюз, закрытый прямой доступ к функции и структурированные логи. Сертификат ISSUED, собственный домен подключён; HTTPS smoke на playground.unicornglade.tech прошёл. Подробности: [CLOUD-STAGE1.md](CLOUD-STAGE1.md).

- [x] Приватная Cloud Function с обёрткой `index.handler`, runtime Node.js 22; реальный health возвращает 200.
- [x] API Gateway вызывает функцию от собственного сервисного аккаунта с `payload_format_version: '2.0'`; анонимный прямой вызов запрещён (403).
- [x] Опубликованы статика и серверный ZIP одной сборки. Проверены HTML, JS/CSS, health и версия на техническом HTTPS-домене; readiness ожидаемо 503 без БД.
- [x] Certificate Manager: через Terraform создан запрос managed-сертификата `fpqlqlsp9augs9lqa7ne`, тип DNS_CNAME, статус при создании VALIDATING.
- [x] Из Terraform получены точные имя/значение записи проверки, сохранена инструкция `DOMAIN-SETUP.md`.
- [x] После согласования переноса DNS создана публичная зона с CNAME сертификата и сохранёнными A корня/`www` и TXT GlobalSign.
- [x] Владелец сменил NS в Руцентре; публичные NS и CNAME проверены через Google DNS-over-HTTPS.
- [x] Сертификат ISSUED.
- [x] Домен привязан к API Gateway, CNAME поддомена создана, HTTPS проверен снаружи (22 сентября).
- [x] Перенос всей DNS-зоны отдельно согласован. Существующие корневые A/TXT сохранены; дальнейшие изменения корня и настройка почты требуют отдельного согласования.

## Этап 4 — БД и полный сценарий

`infra/database/` применён после подтверждения стоимости. Созданы 10 ресурсов (подсеть k не разрешена каталогу и исключена как несозданная). Пароли сгенерированы Connection Manager и внедряются через Lockbox. Кластер STOPPED. Миграции, IAM-чтение счётчика и stop/start подтверждены; HTTPS smoke и инкремент пока не подтверждены из-за сетевых TLS-сбоев. См. [DATABASE-STATUS.md](DATABASE-STATUS.md).

- [x] VPC и подсети a/b/d/e; k недоступна каталогу. Реальное сетевое подключение функций проверено.
- [x] Managed PostgreSQL без публичного IP; доступ TCP/6432 со служебных подсетей функций.
- [x] Один хост на сетевом SSD, backup DONE и защита от удаления.
- [x] Приложение прочитало счётчик через master FQDN, TLS verify-full.
- [x] Приватный IAM-мигратор внутри VPC без публичного HTTP-маршрута; повторная миграция applied: [].
- [x] counter_owner для миграций, counter_app с SELECT/UPDATE без DDL.
- [x] Ограничены масштабирование функции (один экземпляр/одновременный запрос на зону) и время выполнения (10 секунд).
- [ ] Настроить актуальную защиту/частотные лимиты шлюза через Smart Web Security: прежние rate-limit расширения больше не поддерживаются.
- [x] JSON-логи с request ID и версией реально прочитаны из облака; `cloud:logs` ограничен 15 минутами, 30 записями и 64 КБ. Хранение 24 часа.
- [ ] Подготовить переход к Monium: Cloud Logging объявлен к закрытию во II квартале 2027 года.
- [ ] Бюджетные уведомления, ограниченное хранение логов, очистка старых артефактов/версий секретов.
- [ ] Небольшой тестовый ZIP (около 1 ГБ или меньше) в отдельном бакете; проверка HEAD и пробное скачивание с согласованным размером.

## Этап 5 — удобство релизов и приёмка

Предлагаемый интерфейс (**команды ниже, кроме smoke, ещё не реализованы**):

```text
npm run deploy         # сборка → миграции → публикация → smoke
npm run logs           # диагностика; только чтение
npm run status         # адреса, версии, состояние БД
npm run infra:start    # запустить БД и дождаться готовности
npm run infra:stop     # остановить БД, не удаляя данные
npm run rollback       # вернуть совместимый релиз приложения
```

- [ ] Один способ владения версиями функции: CLI и Terraform не должны взаимно откатывать изменения (избежать configuration drift).
- [ ] Идентифицировать релизы, сохранить манифест соответствия сайта и серверной версии.
- [ ] Новый JS/CSS загружается до переключения HTML. Старые хешированные файлы не удаляются сразу.
- [ ] API должен быть совместим со старым фронтендом в момент релиза. Обновление двух сервисов не атомарно.
- [ ] Откат приложения не откатывает автоматически данные/миграции БД; разрушительные миграции требуют отдельного плана.
- [ ] Обычный релиз не пересоздаёт БД, сеть или большие файлы.

Приёмочный сценарий:

1. Развернуть v1, открыть страницу по HTTPS, увеличить счётчик.
2. Проверить в другом браузере, что значение общее.
3. Задеплоить v2 одной командой, проверить новую версию и сохранность счётчика.
4. В контролируемом эксперименте получить ошибку, найти её логи по request ID.
5. Откатить приложение, снова проверить данные.
6. Проверить скачивание архива напрямую из отдельного бакета.
7. Проверить start/stop БД: при остановке статика и архив остаются доступны, API счётчика возвращает ошибку; после запуска данные сохраняются.
8. Только после этого по желанию временно добавить реплики и проверить переключение основного сервера.
